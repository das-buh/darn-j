package com.darnj.parse;

import com.darnj.Error;
import com.darnj.op.*;

final class BlockPat extends Pattern {
    static BlockPat instance = new BlockPat();

    @Override
    Op parse(Parser parser) throws Error {

    }
}
